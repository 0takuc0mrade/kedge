[package]
name = "kedge-core"
version = "0.1.0"
edition = "2021"
description = "Shared types for Kedge agent and ZKP guest program"

[dependencies]
serde = { version = "1", default-features = false, features = ["derive"] }
alloy-sol-types = { version = "1.6", default-features = false }
